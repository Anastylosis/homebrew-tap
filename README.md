# Anastylosis Homebrew tap

```sh
brew install anastylosis/tap/fss
```

Formulae here are generated by each project's release workflow — they are
rendered from the project's `packaging/homebrew/<name>.rb` template with
checksums taken from that release's `SHA256SUMS`. Edit the template in the
project repo, not the formula here, or the next release will overwrite it.

| Formula | Project |
|---------|---------|
| `fss` | [FSS](https://github.com/Anastylosis/FSS) — scrapes scenes and metadata from a studio URL |
